{{- if .Values.pdb }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: failure-analysis-pdb
spec:
  maxUnavailable: 0
  selector:
    matchLabels:
       app.kubernetes.io/name: "failure-analysis"
{{- end }}
